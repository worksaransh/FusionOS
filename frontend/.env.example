# Base URL the frontend calls for the FusionOS API (08_API_STANDARDS.md).
# In local dev this is proxied by Vite (see vite.config.ts); set explicitly for other environments.
VITE_API_BASE_URL=/api/v1
